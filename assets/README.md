Place the MediaPipe Tasks pose model bundle here:

`ai/assets/pose_landmarker_full.task`

The pose wrapper uses this path by default on Python 3.12.
You can override it at runtime with `MEDIAPIPE_POSE_MODEL_PATH`.
